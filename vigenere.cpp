#include <iostream>
#include "vigenere.h"
#include "caesar.h"

std::string encryptVigenere(std::string plaintext, std:: string keywords){
  std::string str="";
  char arr[keyboard.length()];
  strcpy(arr, keyword.c_str());
  return 0;

  for(int i =0, i<plaintext.length[i]i++){
    if(plaintext[i]){
      count++
        
    }
    return;
  }
}